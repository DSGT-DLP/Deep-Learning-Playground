name: Deploy to Vercel
on:
  push:
    branches: [main]
  pull_request:
    branches: [labeled]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: PR Comment
        run: |
          echo "\
            GitHub ref: ${{ github.ref }} \
            PR: ${{ github.event.pull_request.labels }} \
          "
